# Project Summary
Atoms Studio 是一个“对比生成并交付 HTML 应用”的工作台：用户填写需求后，系统生成可离线运行的三文件代码（index.html/style.css/app.js），在预览沙箱中自动执行交互烟测与视觉体检，并将静态校验、运行审计与失败回退/修复形成质量闭环；支持历史版本 diff 审阅、手动修订立即校验，以及一键导出/嵌入到外部页面（srcdoc 内联 iframe）。同时扩展 LLM 运行配置能力：支持 atoms 代理、OpenAI 兼容模式与 mock 降级，并在预览/生成链路展示所选模型信息。

# Project Module Description
- 工作台主页面与会话门禁（/src/pages/Index.tsx）：串联需求输入、LLM 生成/迭代/修复、质量校验、版本保存与恢复；负责预览区切换、嵌入导出与版本 diff 弹窗联动，并把运行设置（含所选模型/模式）透传到下游链路。
- 预览与迭代总控（/src/components/studio/PreviewPane.tsx）：在 iframe 中运行代码并触发 smoke/visual 审计；接收 console/error/unhandledrejection 与资源加载问题，映射到 Quality 面板的修复输入，并展示当前所用模型标签信息。
- 代码工作区（/src/components/studio/CodeWorkspace.tsx）：三文件 html/css/js 分区编辑；支持文件锁定（锁定后生成迭代不覆盖）、dirty 状态提示、应用并保存新版本、以及重置到当前已保存版本。
- 质量面板（/src/components/studio/QualityPanel.tsx）：统一呈现静态校验、交互烟测/视觉体检与运行日志；提供“让 AI 再修复”的入口与补丁来源标识。
- 版本历史对话框（/src/components/studio/VersionHistoryDialog.tsx）：版本时间线、恢复/导出，以及两版本差异对比模式（行级 diff 审阅）。
- 三文件生成与补丁式迭代（/src/lib/llm.ts、/src/lib/pipeline.ts、/src/lib/patch.ts、/src/lib/diff.ts）：
  - llm.ts：根据当前三文件与允许 patch 策略构造提示；可用时优先输出补丁，否则整文件输出，并结合上下文/锁定状态提升可控性。
  - pipeline.ts：质量闭环管线；补丁应用失败则回退整文件重写；在尝试上限内追求通过静态校验。
  - patch.ts：解析模型补丁块并安全应用到目标文件，记录成功/失败明细用于 UI 修复说明。
  - diff.ts：用于版本 diff 的行级差异计算并折叠上下文以提升可读性与性能。
- 嵌入导出（/src/components/studio/EmbedDialog.tsx、/src/lib/embed.ts）：构建 srcdoc 内联 iframe 片段并支持复制；可设置高度与标题说明，并提示片段大小。
- 运行审计（/src/lib/sandboxAudit.ts）：在 iframe 中执行交互烟测与视觉体检，回传结构化问题清单以驱动修复。
- 云端同步与本地数据（/src/lib/cloud.ts、/src/lib/db.ts、/src/lib/session.ts）：db.ts 使用 Dexie 维护应用/版本信息；cloud.ts 负责云端读取/写入与画廊只读读取；session.ts 管理游客本地会话与清理策略。
- 运行设置（/src/lib/settings.ts、/src/components/studio/SettingsDialog.tsx）：
  - 选择 LLM 运行模式：atoms 代理 / OpenAI 兼容 / mock。
  - atoms 模式选择平台白名单模型。
  - OpenAI 兼容模式通过预设填充 baseUrl 与 model，API Key 由用户在本地输入（仅用于浏览器本地配置）。
  - autoFallback 控制不可用时的离线回退策略，并将所选模型用于后续生成链路与 UI 展示。

# Directory Tree
- app/
  - backend/
    - Alembic：studio_apps 数据模型迁移。
    - routers/llm_proxy.py：LLM 代理入口（/api/v1/llm/complete）。
    - schemas/llm_proxy.py：代理请求/响应结构。
    - services/llm_proxy.py：请求白名单、消息过滤、截断、默认回退。
  - frontend/
    - Vite + React + TS：工作台 UI、LLM 适配、IndexedDB 本地持久化、云同步、画廊与会话组件。
    - uploads/
- app/frontend/src/
  - components/studio/
    - CodeWorkspace.tsx：三文件编辑与文件锁定。
    - PreviewPane.tsx：预览 iframe 运行审计与 Quality/Code 状态联动（含模型标签透传）。
    - QualityPanel.tsx：静态校验/烟测/视觉体检/日志与修复入口。
    - VersionHistoryDialog.tsx：版本时间线、恢复/导出与 diff 模式。
    - DiffView.tsx：两版本差异行级展示。
    - EmbedDialog.tsx：生成可嵌入 iframe 片段并复制。
    - SettingsDialog.tsx：配置运行模式与模型预设（atoms/openai/mock）。
  - pages/
    - Index.tsx：主工作台入口与生成/同步/版本控制逻辑（含运行设置接入）。
  - lib/
    - llm.ts：生成/迭代/修复提示构建与补丁/整文件策略（随模式扩展参数）。
    - pipeline.ts：质量闭环生成管线。
    - patch.ts：补丁块解析与安全应用。
    - diff.ts：行级差异计算与折叠上下文。
    - sandboxAudit.ts：iframe 内执行审计并回传问题清单。
    - embed.ts：构建嵌入片段与复制辅助。
    - settings.ts：本地持久化运行设置、模型预设与默认值。
    - codeFiles.ts：渲染/拆分/沙箱桥/文件锁定与相关工具。
    - validator.ts：确定性静态校验。
    - parser.ts：输出解析策略与代码度量。
    - db.ts：Dexie 数据模型与版本追加/恢复/导出。
    - cloud.ts：云同步读取/写入。
    - session.ts：游客会话管理。

# File Description Inventory
- app/backend/services/llm_proxy.py：更新/扩展 LLM 代理支持逻辑以适配新增的运行模式与模型选择参数。
- app/frontend/src/components/studio/PreviewPane.tsx：补齐所需的模型标签导入/透传，使预览与 Quality 展示一致。
- app/frontend/src/components/studio/SettingsDialog.tsx：重构为“运行模式 + 模型预设 + API Key 输入 + 回退开关”的统一设置弹窗。
- app/frontend/src/lib/llm.ts：根据设置模式/模型配置调整调用与提示构建参数。
- app/frontend/src/lib/pipeline.ts：随设置联动生成管线入口参数与调用策略。
- app/frontend/src/lib/settings.ts：新增 LLM 设置结构、atoms 模型白名单元数据、OpenAI 兼容预设与 localStorage 读写。
- app/frontend/src/pages/Index.tsx：把 SettingsDialog 产生的设置接入工作台生成链路并在相关位置透传状态。
- README.md：补充项目产品说明、功能与运行/部署方式的文档内容。
  
# Technology Stack
- 前端：React + TypeScript、Vite、Tailwind CSS、shadcn/ui、Dexie、sonner、lucide-react、CodeMirror
- 云/认证：@metagptx/web-sdk（登录与云端同步）
- 渲染交付：iframe + srcDoc 沙盒运行；postMessage 回传运行时信息；支持嵌入 iframe 片段与单文件导出
- 后端：Python、FastAPI、Alembic（数据迁移）
- LLM：后端代理（/api/v1/llm/complete），支持 atoms 与 OpenAI 兼容模式与 mock 降级

# Usage
## 安装与构建
1. 安装前端依赖：进入前端目录并使用 pnpm（以 package.json scripts 为准）。
2. 构建/开发运行：使用 package.json 中对应的 build/dev 命令。
3. 若需 lint：执行项目 lint（以 package.json scripts 为准）。

## 主要工作流
1. 选择云端登录或游客模式进入工作台（游客模式仅启用本地能力）。
2. 打开设置弹窗选择 LLM 运行模式与模型预设（atoms/OpenAI 兼容/mock），必要时输入 OpenAI 兼容的 API Key；保存后返回主流程。
3. 输入需求并点击生成：系统生成三文件代码并进行静态质量校验；必要时触发补丁/整文件修复重试直到通过或达到上限。
4. 切换到 Preview：在 iframe 运行并自动触发交互烟测与视觉体检；Quality 展示审计结果与修复入口。
5. 在 CodeWorkspace 手动修改三文件后选择应用并保存为新版本；可在版本历史中恢复任意版本并通过 diff 模式审阅差异。
6. 点击嵌入导出生成可复制的 srcdoc iframe 片段，并按需调整高度与标题说明。

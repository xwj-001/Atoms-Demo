# Project Summary
Atoms Studio 是一个“对比生成并交付 HTML 应用”的工作台产品：用户输入需求后，系统生成可离线运行的三文件代码（index.html/style.css/app.js），在预览沙箱中自动执行交互烟测与视觉体检，并通过静态校验、运行审计与失败回退/修复形成质量闭环；同时支持版本 diff 审阅、手动修订后即时校验，以及一键导出/嵌入（srcdoc 内联 iframe）。

# Project Module Description
- 工作台入口与会话门禁（/src/pages/Index.tsx）：承载需求生成/迭代/修复主流程，把认证态与模型/通道配置接入生成与预览链路，并触发当前校验范围的执行与透传。
- 质量面板（/src/components/studio/QualityPanel.tsx）：统一展示静态校验、交互烟测/视觉体检问题与运行日志；根据“校验范围（checkScope）”结果在“AI 再修复”前约束/过滤问题清单。
- 三文件代码生成与预览总控（/src/components/studio/PreviewPane.tsx）：在 iframe 中运行并触发 smoke/visual 审计，通过页面间消息回传运行问题与日志，并把校验范围设置透传到质量面板侧。
- 生成与校验编排（/src/lib/pipeline.ts、/src/lib/validator.ts、/src/lib/settings.ts）：编排生成流程、执行校验规则与策略选择；统一“校验范围”的默认策略与触发时机，收敛 TODO/占位内容的误判处理到范围控制链路。
- 工作区与设置（/src/components/studio/SettingsDialog.tsx）：模型与通道配置选择；同步校验范围开关/默认策略到生成与校验链路，确保设置变更即时生效。

# Directory Tree
- app/
  - backend/
    - alembic/versions/：账号/作品相关数据库迁移
    - data_models/：后端数据模型与账号表结构
    - dependencies/：鉴权依赖（解析 X-Studio-Token）
    - models/：账号与作品模型（含密码哈希与作品归属隔离）
    - routers/
      - studio_auth.py：注册/登录/me/改密
      - studio_data.py：作品 CRUD 与画廊读取（按账号归属校验）
      - llm_proxy.py：LLM 代理入口（/api/v1/llm/complete）
    - services/
      - studio_auth.py：账号注册/登录/改密（PBKDF2-HMAC-SHA256 + JWT）
      - deepseek_proxy.py：DeepSeek 代理实现
  - frontend/
    - src/
      - components/studio/
        - AccountMenu.tsx：账号菜单（同步/改密/登出）
        - LoginGate.tsx：登录/注册/游客门禁
        - ChangePasswordDialog.tsx：自建账号改密
        - PreviewPane.tsx：iframe 运行、烟测/体检与日志回传（校验范围透传）
        - QualityPanel.tsx：质量问题展示与修复触发（按校验范围过滤）
        - GalleryPanel.tsx：作品画廊/展示面板（iframe sandbox 限制兼容）
        - VersionHistoryDialog.tsx、DiffView.tsx：版本与 diff 审阅
        - EmbedDialog.tsx：srcdoc 嵌入导出
        - SettingsDialog.tsx：模型与通道配置（校验范围开关注入）
      - lib/
        - studioAuth.ts：本地 token、X-Studio-Token 携带与请求封装
        - cloud.ts：云同步接口
        - llm.ts：提示/模式文案拼装与生成管线策略
        - pipeline.ts：生成流程编排（校验范围默认/触发时机）
        - sandboxAudit.ts：沙盒审计执行（iframe 兼容）
        - settings.ts：默认配置（校验范围默认）
        - validator.ts：校验规则与范围判断（基于范围限定检查）
        - codeFiles.ts：三文件代码拼装与复用片段
        - embed.ts：srcdoc 嵌入/导出构建
        - templates.ts：运行模板/片段生成（供沙盒审计与代码组装复用）

# File Description Inventory
- app/frontend/src/components/studio/SettingsDialog.tsx：配置入口；同步模型/通道与校验范围开关/默认策略到生成与校验链路。
- app/frontend/src/pages/Index.tsx：工作台入口；把认证态与生成/预览执行入口串起来，并纳入校验范围执行链路。
- app/frontend/src/lib/sandboxAudit.ts：沙盒审计执行与运行时校验适配（本次变更涉及模板化交互与审计一致性）。
- app/frontend/src/lib/templates.ts：运行/交互相关模板与片段定义（本次变更涉及按钮样式与可交互区域的统一）。

# Technology Stack
- 前端：React + TypeScript、Vite、Tailwind CSS、shadcn/ui、Dexie、sonner、lucide-react、CodeMirror、@metagptx/web-sdk
- 交付/渲染：iframe + srcDoc；postMessage 回传运行信息；支持嵌入与导出
- 后端：Python、FastAPI、Alembic
- 认证与账号：自建 PBKDF2-HMAC-SHA256 + JWT（X-Studio-Token）
- LLM：后端代理（/api/v1/llm/complete），含 DeepSeek 通道

# Usage
1. 安装与构建：分别进入前端/后端目录按项目约定安装依赖并执行构建；同时通过 lint 与类型/构建校验。
2. 运行工作台：启动前后端服务后进入工作台，完成需求生成、预览审计、质量面板问题查看与（如需要）AI 再修复/导出/嵌入流程测试。
